Our CPP Assignment Repository
-----------------------------

Git commands:

in the remote repo folder, to create new repo

git init

then make repo bare use:

git config --bool core.bare true

cd to local folder

git clone <address of remote repo>

add to GIT app

==
